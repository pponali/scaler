package com.practice.module2.day7;

/**
 * @Author prakashponali
 * @Date 13/11/23
 * @Description
 *
 * The prefix sum formula for an array A of N integers is given by :-
 *
 * //prefSum[i] = prefSum[i] + A[i]
 * //prefSum[i] = prefSum[i - 1] + A[i] - right answer
 * //prefSum[i - 1] = prefSum[i] + A[i]
 * //prefSum[i - 1] = prefSum[i - 1] + A[i]
 */
public class Prefix_Sum_Formula {
}
